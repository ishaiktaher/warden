"""Optional first-party infrastructure adapters.

Modules in this package import vendor SDKs only when their provider is selected,
so the Warden core remains vendor-neutral and has no mandatory cloud dependency.
"""
